# Plan: Merge DAS Content with Quantum Styling

## Goal
Replace `src/App.jsx` content with the up-to-date DAS content from `das_earthquake_warning_site.jsx`, while preserving the quantum version's styling (colors, card designs, gradients, animations, and overall visual polish).

## Key Changes

### 1. Content Replacement
- Keep full DAS site structure and text content
- Preserve DAS site's Interactive City Impact Comparison (the map + comparison table)
- Update messaging from "quantum" to "DAS/standardized fiber network" focus
- Use DAS site's city data and comparison metrics

### 2. Styling Application (from quantum version)
- Background: `bg-slate-950` with cyan/violet radial gradient overlays
- Card styling: `rounded-2xl/3xl`, `border border-white/10`, `bg-white/5`, shadows
- Color scheme: cyan-400/300, violet-400 accents (not the DAS site's solid colors)
- Typography: uppercase tracking styles, specific font weight hierarchy
- Animations: Add `framer-motion` to section transitions and elements
- Header: sticky with backdrop-blur, gradient logo ring
- Buttons: rounded-full with proper hover/active states
- Inputs: styled with borders, focus rings, proper placeholders

### 3. Structural Mapping
```
Hero section → DAS hero content, quantum styling
City Impact Map → DAS interactive map, styled with quantum card design
Objectives section → DAS objectives, quantum card grid
Team section → DAS roles array, quantum role cards
Join section → DAS form fields, quantum input styling
```

### 4. Dependency Update
Add `react-simple-maps` to package.json since the Interactive City Impact Comparison requires it.

### 5. Navigation
Use DAS site's simpler nav structure (Map, Objectives, Team, Join) but styled with quantum approach.

## Implementation Steps
1. Install `react-simple-maps` dependency
2. Build new App.jsx combining:
   - DAS site's content, data, and component logic
   - Quantum site's styling patterns, animations, and visual polish
3. Ensure all sections render with consistent quantum aesthetic
4. Test build and dev server